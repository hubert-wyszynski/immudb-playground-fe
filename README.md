# Immudb Playground Frontend

## Prerequisites
- Node.js (v18.x)
- Docker

## Running the application
1. Run `docker compose up` to start the frontend container.
2. Open [http://localhost:3000](http://localhost:3000) in your browser.